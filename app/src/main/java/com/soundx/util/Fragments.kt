package com.soundx.util

enum class Fragments {
    HOME_FRAGMENT,
    LIBRARY_FRAGMENT,
    SEARCH_FRAGMENT,
    CREATE_PLAYLIST_FRAGMENT,
    EDIT_PLAYLIST_FRAGMENT
}

enum class DialogFragments{
    PLAYLIST_DIALOG_FRAGMENT
}